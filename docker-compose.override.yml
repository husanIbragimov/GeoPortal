
services:
  nginx:
    image: nginx:1.27
    container_name: gis-nginx
    networks:
      - shared_network
    ports:
      - "8001:80"
    volumes:
      - ./.conf/docker-nginx.conf:/etc/nginx/nginx.conf
      - ./static:/app/static
      - ./media:/app/media
    depends_on:
      - web
